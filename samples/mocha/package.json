{
  "name": "testmybot-sample-mocha",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "emulator": "../../node_modules/.bin/botium-cli emulator console --convos ./spec/convo --config ./testmybot.json",
    "emulatorBrowser": "../../node_modules/.bin/botium-cli emulator browser --convos ./spec/convo --config ./testmybot.json"
    "test": "mocha --reporter spec --exit spec"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.0.2",
    "mocha": "^4.0.1",
    "testmybot": "../.."
  },
  "dependencies": {
    "botkit": ">=0.5.4 <1.0.0"
  }
}
