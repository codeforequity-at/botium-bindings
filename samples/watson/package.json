{
  "name": "botium-bindings-sample-watson",
  "version": "1.0.0",
  "scripts": {
    "test": "mocha --reporter mochawesome --reporter-options \"code=false,reportTitle=IBM Watson Conversation Sample\"  --exit spec",
    "emulator": "npm install botium-cli && botium-cli emulator console --convos ./spec/convo",
    "emulatorBrowser": "npm install botium-cli && botium-cli emulator browser --convos ./spec/convo"
  },
  "devDependencies": {
    "chai": "latest",
    "mocha": "latest",
    "mochawesome": "latest",
    "botium-connector-watson": "latest",
    "botium-bindings": "latest"
  },
  "author": "Botium GmbH",
  "license": "ISC"
}
